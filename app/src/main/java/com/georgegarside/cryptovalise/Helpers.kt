package com.georgegarside.cryptovalise

import android.support.annotation.IdRes
import android.support.v4.app.Fragment
import android.support.v4.app.FragmentActivity

/*fun FragmentActivity.fragmentReplace(@IdRes containerViewId: Int, fragment: Fragment) {
	this.supportFragmentManager.beginTransaction().replace(containerViewId, fragment).commit()
}
*/
